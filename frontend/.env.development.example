# Copy to `.env.development` if you need overrides (file is gitignored).
# Vite reads `../backend/.env` for the `/api` proxy target first (PORT / BACKEND_URL).
# Only uncomment if backend/.env is missing — port must match Express.
# VITE_PROXY_TARGET=http://127.0.0.1:5001
